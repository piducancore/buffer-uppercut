{
  "name": "buffer-uppercut-site",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --test tests/*.test.mjs",
    "test:build": "node scripts/check-build.mjs"
  },
  "engines": { "node": ">=22.12.0" },
  "dependencies": { "astro": "7.3.2" }
}
